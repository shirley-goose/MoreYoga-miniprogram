<!-- 师资介绍页面 -->
<!-- 背景图 -->
<image class="global-bg" src="../../images/background.png" mode="aspectFill"/>

<!-- 自定义导航栏 -->
<view class="nav-bar">
  <view class="nav-left">
    <!-- 返回键 -->
    <view class="back-btn" bindtap="goBack">
      <view class="back-icon">←</view>
    </view>
  </view>
  <view class="nav-title">墨瑜伽</view>
  <view class="nav-right"></view>
</view>

<view class="page-container">
  <view class="container">
    
    <!-- 老师头像和姓名 -->
    <view class="teacher-header">
     <div class="teacher-info">
      <view class="teacher-name">{{teacherInfo.name}}</view>
      <div class="teacher-subtitle">{{teacherInfo.intro}}</div>
     </div>
      <image class="teacher-photo" src="{{teacherInfo.photo}}" mode="aspectFill"/>
    </view>

    <!-- 她眼中的瑜伽板块 -->
    <view class="words-section">
      <view class="section-divider"></view>
      <view class="section-title">她眼中的瑜伽</view>
      <view class="teacher-words">{{teacherInfo.words}}</view>
    </view>


    <!-- 关于课程板块 -->
    <view class="course-section">
      <view class="section-divider"></view>
      <view class="section-title">擅长课程</view>
      <view class="course-content">
        <view wx:for="{{teacherInfo.specialties}}" wx:key="index" class="course-item">
          <view class="course-category">【 {{item.category}} 】</view>
          <view class="course-description">{{item.description}}</view>
        </view>
      </view>
    </view>

  </view>
</view>